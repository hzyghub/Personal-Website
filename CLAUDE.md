# CLAUDE.md

> 版本：v2.0 | 最后更新：2026-04-16 | 审查触发条件：技术栈变更 / 新阶段启动 / 内容信息变更

## 项目概述

黄朝阳（Huang Chaoyang）的个人简历/作品集网站。综合在线名片，面向HR、技术面试官、行业同行。中英双语，温和质感卡片流设计。

## 技术栈

- **前端**：纯 HTML5 + CSS3 + Vanilla JS（零依赖、零CDN、零构建工具）
- **部署**：GitHub Pages（静态托管）
- **字体**：系统字体栈（不加载外部字体）
- ~~不使用 React / Vue / 任何框架~~（纯静态站）
- ~~不使用 Bootstrap / Tailwind CDN~~（手写CSS）

## 架构概述

```
单页面应用（index.html）— 卡片流布局
├── 导航栏（固定顶部，白底+阴影）
├── Hero 卡（全屏居中，头像+名字+标签+CTA）
├── 关于我 + 技能（并排双卡）
├── AFC 项目大卡（全宽）
├── 二手车预测 + 资金流预测（并排小卡）
├── IntentBridge 达意（全宽横卡 + AI理念）
├── 履历（横向年份卡片网格）
└── 联系方式（居中邮箱）
```

### 文件结构

```
/
├── index.html          # 主页面（卡片流布局）
├── css/
│   └── style.css       # 全部样式（含响应式+动画）
├── js/
│   └── main.js         # 语言切换、导航、滚动动画
├── assets/
│   └── images/         # 头像照片
└── docs/
    └── PRD.md          # 产品需求文档
```

## 当前状态

- 阶段：v2.0 视觉重设计完成
- 设计文档：`docs/superpowers/specs/2026-04-16-visual-redesign-design.md`

## 关键约束

### 设计约束（不可妥协）
- 配色：暖白 `#fafaf8`，青绿 `#2a9d8f`，珊瑚 `#e76f51`
- 所有动态效果必须用纯 CSS 或轻量 JS 实现，禁止引入动画库
- 首屏加载 < 2s，总大小 < 500KB（不含图片）
- 零外部依赖——不加载任何 CDN 资源

### 内容约束
- 中英双语，默认中文，右上角切换
- 所有中文内容必须有对应英文翻译
- 联系方式：邮箱 hzymicro@outlook.com（已确认）
- 头像：使用 `assets/images/avatar.jpg`（圆形裁剪，青绿浅色边框）

### 无障碍
- 色彩对比度满足 WCAG AA
- 键盘可导航
- 合理的 alt 文本和 aria 标签

## 范围边界（v1.0 MVP）

**范围内**：
- 卡片流布局（Hero、关于我+技能、项目卡片、AI理念、履历、联系）
- 中英双语切换
- 响应式（桌面/平板/手机）
- 滚动入场动画
- 卡片 hover 微动效
- SEO meta标签 + Open Graph

**范围外**（v2.0 候选）：
- 博客/文章系统
- 暗色/亮色主题切换
- 项目详情弹窗/子页面
- 简历PDF在线预览
- Google Analytics
- 自定义域名配置

**遇到范围外需求**：告知用户属于v2.0候选，不实现。用户坚持则标注后可实现。

**遇到灰色地带需求**：暂停，向用户确认是否属于v1.0范围。

## 术语一致性

| 术语 | 标准写法 | 说明 |
|------|---------|------|
| 卡片流 | Card Flow | 本站布局方式代号 |
| 规格工程 | Spec Engineering | IntentBridge方法论的英文名 |
| 活标准审查 | Living Standard Review | 达意项目的核心审查方法 |

## 按任务类型的文件触发规则

| 任务类型 | 必读文件 |
|---------|---------|
| 实现任何板块 | `docs/PRD.md`（对应板块的内容和视觉规范） |
| 修改配色/样式 | `docs/PRD.md` 第2.1节（设计决策） |
| 添加/修改项目内容 | `docs/PRD.md` 第3.4节（项目展示） |
| 修改简历信息 | `docs/PRD.md` 第8节（已确认内容） |

## AI 协作规则

### 规则优先级（冲突时按此顺序）
1. **零外部依赖**：不引入任何CDN或npm包
2. **PRD一致性**：内容以 `docs/PRD.md` 为准，不自行编造
3. **性能优先**：动画和效果不能影响首屏加载速度
4. **最小改动**：只改当前任务涉及的文件

### 启动任务时
1. 读取 `docs/PRD.md` 对应板块
2. 确认当前任务的范围
3. 不做范围外扩展

### 实现要求
- CSS写在 `css/style.css` 中，按板块用注释分区
- JS写在 `js/main.js` 中，按功能模块组织
- HTML结构语义化，使用 `<section>` + `id` 做锚点
- 响应式用 `@media` 断点：1200px / 768px
